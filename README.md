# freeswitch module global_vars

this module only set global variables on freeswitch,
i use it with xml_curl.

# usage

set variables in **conf/global_vars.conf.xml**, then you can get value using $${mivariablename}.
